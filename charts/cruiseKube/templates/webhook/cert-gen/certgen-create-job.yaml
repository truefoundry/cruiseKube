{{- if and .Values.cruiseKubeWebhook.enabled .Values.cruiseKubeWebhook.certGen.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "cruiseKubeWebhook.fullname" . }}-certgen-create
  labels:
    {{- include "cruiseKubeWebhook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      name: {{ include "cruiseKubeWebhook.fullname" . }}-certgen-create
      labels:
        {{- include "cruiseKubeWebhook.labels" . | nindent 8 }}
    spec:
      restartPolicy: OnFailure
      serviceAccountName: {{ include "cruiseKubeWebhook.certGenServiceAccountName" . }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 2000
        fsGroup: 2000
      containers:
      - name: cert-gen
        image: {{ .Values.cruiseKubeWebhook.certGen.image.repository }}:{{ .Values.cruiseKubeWebhook.certGen.image.tag }}
        imagePullPolicy: {{ .Values.cruiseKubeWebhook.certGen.image.imagePullPolicy }}
        args:
        - create
        - --host={{ include "cruiseKubeWebhook.webhookServiceName" . }},{{ include "cruiseKubeWebhook.webhookServiceName" . }}.{{ .Release.Namespace }}.svc,127.0.0.1,localhost
        - --namespace={{ .Release.Namespace }}
        - --secret-name={{ include "cruiseKubeWebhook.tlsSecretName" . }}
        - --cert-name=tls.crt
        - --key-name=tls.key
        - --log-level=debug
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
        resources:
          {{- toYaml .Values.cruiseKubeWebhook.certGen.resources | nindent 10 }}
{{- end }}
