{{- if and .Values.autopilotController.enabled .Values.autopilotController.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "autopilotController.fullname" . }}
  labels:
    {{- include "autopilotController.labels" . | nindent 4 }}
rules:
- apiGroups: ["*"]
  resources: ["*"]
  verbs: ["*"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "autopilotController.fullname" . }}
  labels:
    {{- include "autopilotController.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "autopilotController.fullname" . }}
subjects:
- kind: ServiceAccount
  name: {{ include "autopilotController.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
{{- end }}
