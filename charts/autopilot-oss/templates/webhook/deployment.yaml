{{- if .Values.autopilotWebhook.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "autopilotWebhook.fullname" . }}
  labels:
    {{- include "autopilotWebhook.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.autopilotWebhook.replicas }}
  selector:
    matchLabels:
      {{- include "autopilotWebhook.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- with .Values.autopilotWebhook.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "autopilotWebhook.labels" . | nindent 8 }}
        {{- with .Values.autopilotWebhook.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.autopilotWebhook.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "autopilotWebhook.serviceAccountName" . }}
      containers:
        - name: {{ .Chart.Name }}-webhook
          image: "{{ .Values.autopilotWebhook.image.repository }}:{{ include "autopilotWebhook.imageTag" . }}"
          imagePullPolicy: {{ .Values.autopilotWebhook.image.pullPolicy }}
          command: ["./autopilot"]
          args:
            - "--execution-mode=webhook"
            - "--webhook-certs-dir={{ .Values.autopilotWebhook.webhook.certsDir }}"
            - "--webhook-stats-url-host={{ .Values.autopilotWebhook.webhook.statsURL.host }}"
          ports:
            - name: https
              containerPort: 8443
              protocol: TCP
            - name: metrics
              containerPort: 8081
              protocol: TCP
          resources:
            {{- toYaml .Values.autopilotWebhook.resources | nindent 12 }}
          env:
            {{- range $key, $value := .Values.autopilotWebhook.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          volumeMounts:
            - name: webhook-certs
              mountPath: {{ .Values.autopilotWebhook.webhook.certsDir }}
              readOnly: true
      volumes:
        - name: webhook-certs
          secret:
            secretName: {{ include "autopilotWebhook.tlsSecretName" . }}
{{- end }}
